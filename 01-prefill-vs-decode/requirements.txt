transformers>=4.40
torch>=2.2

# vLLM is deliberately NOT pinned here. It ships no Windows wheels (verified
# against PyPI through 0.27.1 - manylinux only), so `pip install -r
# requirements.txt` on native Windows would just fail. If you're running
# this module from Linux or WSL2, install it separately:
#   pip install vllm
# vllm_prefill_decode.py and benchmark.py both detect whether it's
# importable and report clearly if it isn't, instead of failing hard.
